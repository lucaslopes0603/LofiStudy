services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: lofi_study
      POSTGRES_USER: lofi
      POSTGRES_PASSWORD: lofi
    ports:
      - "5432:5432"
    volumes:
      - lofi_postgres:/var/lib/postgresql/data

volumes:
  lofi_postgres:
